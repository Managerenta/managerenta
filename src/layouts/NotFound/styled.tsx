"use client";
import styled from "styled-components";
import { Box } from "../../components";

export const NotFoundStyled = styled(Box)`
	max-width: 300px;
	max-height: 100px;
	background: var(--Off-White);
	margin: 0 auto;
	color: var(--Black);
	padding: 10px;
	border-radius: 8px;
	box-shadow: 2.5px 2.5px 2.5px var(--White);
`;

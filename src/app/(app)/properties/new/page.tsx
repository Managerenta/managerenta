import { Suspense } from "react";
import AddPropertyWrapper from "@/libs/AddPropertyWrapper";

export default function AddPropertyPage() {
	return (
		<Suspense>
			<AddPropertyWrapper />
		</Suspense>
	);
}
